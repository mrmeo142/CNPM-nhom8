extends layout 

block content

    h1=title

        p Do you really want to delete this item?
        form(method='POST')
            div.form-group
                input#foodid.form-control(type='hidden', name='foodid', value=food._id)
            button.btn.btn-primary(type='submit') Delete